TaskRunner is a utility to run a set of tasks while respecting the ordering
of tasks such that tasks with the same order executes concurrently.

See com.vimukthi.taskrunner.tests.TestMain for an example.